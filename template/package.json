{
  "name": "<@= projectName @>",
  "version": "1.0.0",
  "scripts": {
    "start": "gulp dev",
    "build": "gulp build"
  },
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.26.5",
    "fancy-log": "^1.3.3",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^6.1.0",
    "gulp-changed": "^3.2.0",
    "gulp-clean": "^0.4.0",
    "merge-stream": "^2.0.0",
    "gulp-jsbeautifier": "^3.0.0",
    "gulp-plumber": "^1.2.1"<@ if (html === 'pug') { @>,
    "gulp-pug": "^4.0.1"<@ } @><@ if (css === 'sass' || css === 'scss') { @>,
    "gulp-sass": "^4.0.2"<@ if (css === 'sass') { @>,
    "sass-convert": "^0.5.2"<@ } @><@ } else if (css === 'postcss') { @>,
    "gulp-postcss": "^8.0.0"<@ } @><@ if (js === 'coffee') { @>,
    "gulp-coffee": "^3.0.3"<@ } else if (js === 'typescript') { @>,
    "gulp-typescript": "^5.0.1"<@ } @>
  }
}
